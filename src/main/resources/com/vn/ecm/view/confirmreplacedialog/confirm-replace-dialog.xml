<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<view xmlns="http://jmix.io/schema/flowui/view"
      title="Confirm Replace Permissions">
    <layout>
            <span id="messageSpan"
                  text="⚠ This will replace explicitly defined permissions on all descendants of this object with inheritable permissions from"/>
            <label text="Do you wish to continue?"/>

            <hbox spacing="true" alignSelf="END">
                <button id="noBtn" text="No"/>
                <button id="yesBtn" text="Yes" themeNames="primary"/>
            </hbox>

    </layout>
</view>